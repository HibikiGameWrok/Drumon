using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class transformAbilitySheet : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //this.transform.rotation = new Quaternion(0.0f, -40.0f, 0.0f, 0.0f); 
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
